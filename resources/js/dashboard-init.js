// $(document).ready(function() {
//     $("textarea").summernote({
//         minHeight: 400 // set minimum height of editor
//     });
// });

$("textarea").summernote({
    placeholder: "Hello bootstrap 4",
    tabsize: 2,
    height: 100
});
